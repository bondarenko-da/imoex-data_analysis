# SQLite (recommended for Vercel, works offline)
DATABASE_URL=sqlite:
# PostgreSQL (if you have a hosted PostgreSQL)
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require

MOEX_ISS_BASE=https://iss.moex.com/iss
